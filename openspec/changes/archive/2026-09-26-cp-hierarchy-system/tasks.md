## 1. Data model & storage foundations

- [x] 1.1 In `lib/adminStorage.js`, add `ADMIN_KEYS.cpAssignments` and `ADMIN_KEYS.cpSiteVisits` (empty-array collections) — also registered in `lib/adminAxios.js`'s `COLLECTION_MAP` so the generic `/admin/cp-assignments` and `/admin/cp-site-visits` REST-over-localStorage calls work.
- [x] 1.2 In `lib/adminStorage.js`, add helper functions: `addCpAssignment(assignment)`, `getActiveAssignmentFor(propertyId, level)`, `addCpSiteVisit(visit)`, `updateCpSiteVisit(visitId, patch)`, `forwardLeadToCompanyCp(leadId, companyCpName)`, `delegateLead(leadId, cpType, cpName)`, `addCampaignVideo(video)`.
- [x] 1.3 Add `routingStage` (default `"head-cp"`) to the lead shape used by `lib/adminStorage.js`'s `addCpLead`. No backfill needed — `ADMIN_KEYS.cpLeads` always seeds to `[]` (leads only ever arrive via real "Forward to Head CP" actions), and the generic `LEAD_COLUMNS` filter defaults any lead missing the field to `"head-cp"` (see 2.1).
- [x] 1.4 Add `routingStage` to `CP_LEADS` in `lib/demoPortal.js` for every demo lead record (also added a missing "Simnani Partners Pvt. Ltd." Company CP to `CP_NETWORK` — the feature has no path to work without at least one seeded Company CP to assign properties/leads to).
- [x] 1.5 Add `CP_PROJECT_ASSIGNMENTS` demo data to `lib/demoPortal.js` covering `head-to-company`, `company-to-field`, `company-to-digital`, referencing real `lib/properties.js` IDs (`buy-1`, `sell-1`).
- [x] 1.6 **Corrected during implementation**: `app/api/leads/*` is the unrelated buyer→broker property-inquiry system (MongoDB `Lead` model, `ownerId`/`buyerId`), not the CP-leads store — it has no `routingStage` concept and does not need one. The CP-leads store is `ADMIN_KEYS.cpLeads` (localStorage only, per the rest of the CP feature); no backend/API route exists or is needed for it. No code change required here.

## 2. Lead routing: Head CP inbox → Company CP → Field/Digital CP

- [x] 2.1 In `components/admin/freelancer-cp/CPTypeWorkspace.jsx`, change the `leads` filter from `effectiveLeadCpTypes.includes(submittedBy?.cpType)` to a `routingStage`-based filter (`lead.routingStage === stage`, defaulting missing values to `"head-cp"`).
- [x] 2.2 Add a `routingStage` prop to `CPTypeWorkspace`, and update `app/(admin)/admin/freelancer-cp/{head-cp,company,digital,field}/page.js` to pass the correct value. Also fixed the `/admin/freelancer-cp` overview page's per-segment lead counts (`CP_SEGMENTS`), which had the same `submittedBy.cpType`-based miscount — now uses `routingStage` too.
- [x] 2.3 Added inline in the Pipeline column (simpler than a separate dialog, consistent with the existing inline-select pattern): when `routingStage === "head-cp"`, a "Forward to Company CP…" select lists active Company CPs; choosing one patches the lead to `routingStage: "company-cp"`.
- [x] 2.4 Extended the pipeline "Assign to…" select into a two-step chooser (optgroups: Field CP / Digital CP) shown only when `routingStage === "company-cp"`; choosing a partner sets `routingStage` to `field-cp`/`digital-cp` and `status: "Assigned"`.
- [x] 2.5 Added a "Pending Forward" KPI card shown only on the Head CP workspace (`routingStage === "head-cp"`).
- [x] 2.6 `CompanyCPDashboard.jsx`'s "Assigned Projects" tab now reads real `cpAssignments` (see 3.6) instead of purely local demo state; the "Freelancer Leads" tab is left as a read-only report on the `leads` prop (no actions live there, so no routing-consistency risk). Digital/Field CP's own "Forward to Head CP" buttons already wrote into the real `ADMIN_KEYS.cpLeads` store before this change — that half of the loop worked correctly already.

## 3. Property/project assignment chain

- [x] 3.1 Create `components/admin/freelancer-cp/AssignPropertyDialog.jsx` — a reusable dialog that lists active CPs of a given type and creates a `cpAssignment` record via `addCpAssignment`. Reused for both Head→Company (properties page) and Company→Field/Digital (CPTypeWorkspace) delegation.
- [x] 3.2 In `app/(admin)/admin/properties/page.js`, added an "Assign to Company CP" row action (alongside View/Edit/Delete) that opens `AssignPropertyDialog` scoped to Company CPs, and a "CP Assignment" column badge ("Unassigned" / "Assigned: <name>").
- [x] 3.3 Added an "Assigned Projects" tab to `CPTypeWorkspace` (`showAssignedProjects` + `assignmentLevel` props), listing `cpAssignments` at that level — enabled on `company` (`head-to-company`), `digital` (`company-to-digital`), and `field` (`company-to-field`) pages. Note: this is an admin oversight table across the whole CP-type segment (all partners of that type), matching how the rest of `CPTypeWorkspace` already works — not a single partner's personal view.
- [x] 3.4 On the Company CP page's "Assigned Projects" tab, added a "Delegate" / "Re-delegate" action (`delegateToTypes={["field","digital"]}`) that reopens `AssignPropertyDialog` scoped to the company's Field+Digital network and creates a `company-to-field`/`company-to-digital` record with `parentAssignmentId` set.
- [x] 3.5 Field CP and Digital CP pages scope their "Assigned Projects" tab via `assignmentLevel="company-to-field"` / `"company-to-digital"` — no delegate action shown (terminal stage, `delegateToTypes` omitted).
- [x] 3.6 Mirrored on the public portal: `CompanyCPDashboard.jsx`'s "Assigned Projects" tab now reads real `cpAssignments` (`head-to-company`, scoped to `partner.fullName`) and delegates for real via `addCpAssignment`, replacing the old projects-prop + local-only-state mock. `FieldCPDashboard.jsx`'s "Assigned Projects" tab gained a "Properties Delegated by Company CP" section reading `company-to-field` assignments (kept the existing lead-grouped view below it unchanged — it already worked). `DigitalCPDashboard.jsx` gained a new "Assigned Projects" tab entirely (didn't exist before) reading `company-to-digital` assignments. `FreelancerPortalClient.jsx` now passes `partner` to `CompanyCPDashboard` (it didn't before, unlike Digital/Field) so it knows which Company CP it's acting as.

## 4. Field CP workflow

- [x] 4.1 Created `components/admin/freelancer-cp/SiteVisitLogDialog.jsx` — handles both scheduling (customer, phone, date/time) and outcome logging (Attended/No-show + notes) via a `mode` prop.
- [x] 4.2 Added a "Site Visits" tab to `CPTypeWorkspace` (`showSiteVisits` prop, enabled only on the Field CP page) backed by `ADMIN_KEYS.cpSiteVisits`, with a header "Schedule Site Visit" action.
- [x] 4.3 Wired "Schedule Visit" from both an assigned-project row (`ASSIGNED_PROJECT_COLUMNS`, when `assignmentLevel === "company-to-field"`) and a `routingStage: "field-cp"` lead row (Pipeline cell) into `SiteVisitLogDialog`, creating a `cpSiteVisit` record via `addCpSiteVisit`.
- [x] 4.4 Wired visit-outcome logging (`updateCpSiteVisit`) to update the linked lead's `status` (`"Site Visit Scheduled"` → `"Site Visit Completed"` on Attended; stays at `"Site Visit Scheduled"` with a no-show note otherwise).
- [x] 4.5 The existing lead pipeline `<select>` already lets any CP move a lead through the full `CP_LEAD_STATUSES` enum to `Converted`/`Lost` — note the data model has no separate `"Negotiation"` status (it goes `Site Visit Completed` → `Converted`/`Lost` directly); didn't add one, to avoid changing an enum used elsewhere without being asked. Added: patching a lead's status to `"Converted"` now auto-creates a `commissions` record (`status: "Pending"`) if one doesn't already exist for that lead.

## 5. Digital CP workflow

- [x] 5.1 There was no partner-facing video upload UI at all before this change (only admin-side moderation existed). Added one: `DigitalCPDashboard.jsx`'s new "Assigned Projects" tab lists only properties with an active `company-to-digital` assignment for that partner, each with an upload field that calls the new `addCampaignVideo()` helper — structurally impossible to upload against a non-assigned project since the list only ever shows assigned ones.
- [x] 5.2 Confirmed: `VideoModerationDialog` approve/suggest-edit flow is on the Company CP admin workspace (`showCampaignVideos` prop, unchanged) and still scopes correctly after the routing changes — verified by reading `CPTypeWorkspace.jsx`'s `campaignVideos` memo, which is untouched by this change.
- [x] 5.3 Added a per-project performance badge on the Digital CP's "Assigned Projects" tab showing lead count generated (matching `submittedBy.name === partner.fullName`, grouped by `lead.project`). **Simplified**: commission-approval-status per project was dropped from this view — `DigitalCPDashboard` has no `commissions` prop today and adding one was out of proportion to the rest of this change; lead count alone gives a meaningful performance signal.

## 6. Verification & cleanup

- [ ] 6.1 **Not verified interactively** — no browser automation tool was available in this session. Verified structurally instead: `npm run build` compiles all routes (including all four `/admin/freelancer-cp/*` pages, `/admin/properties`, `/portal/freelancer`) with zero errors, and a dev-server smoke test (`curl`) confirmed every route returns 200 with no server-side errors/warnings in the log. The actual click-through (add property → assign → delegate → visit → convert → commission; digital branch upload → moderate → track) was reasoned through code-by-code but not clicked live. **Recommend running this manually before shipping.**
- [ ] 6.2 Same caveat as 6.1 — verified by code inspection (the `routingStage` filter and forward/delegate handlers were traced end-to-end), not by live interaction.
- [ ] 6.3 Same caveat — admin and portal both read the same `ADMIN_KEYS.cpAssignments` / `cpLeads` localStorage collections via `readCollection`, so they are structurally guaranteed to agree (same source of truth), but this was not visually confirmed in two open tabs.
- [ ] 6.4 **Not verified** — no browser available to check 375px/768px/1440px breakpoints visually. All new markup reuses existing responsive utility patterns from the codebase (`AdminTable`, `AdminDialog`, `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`, etc.) rather than introducing new layout primitives, which lowers risk but doesn't substitute for a visual check.
- [x] 6.5 Ran `npm run lint` — zero new errors/warnings in any file touched by this change (all 15 pre-existing errors are in unrelated files: `AdminAuthContext.jsx`, `AdminThemeContext.jsx`, `AuthContext.jsx`, `lib/savedProperties.js`, `lib/useWizardDraft.js`, `public/ffmpeg/worker.js`, and other untouched admin pages). Fixed a `react-hooks/set-state-in-effect` error introduced by every new data-loading `useEffect` I added, by following the codebase's existing `Promise.resolve().then(...)` convention (already used in `CPTypeWorkspace.jsx`'s original `load()` effect).
